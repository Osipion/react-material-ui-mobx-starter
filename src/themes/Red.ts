import {ThemeOptions} from "@material-ui/core";
import {red} from "@material-ui/core/colors";

// A sample custom theme that sets the primary palette to red
const theme: ThemeOptions = {
    palette: {
        primary: red
    }
};

export default theme;