import {ThemeOptions} from "@material-ui/core";
import {blue} from "@material-ui/core/colors";

// A sample custom theme that sets the primary palette to blue
const theme: ThemeOptions = {
    palette: {
        primary: blue
    }
};

export default theme;